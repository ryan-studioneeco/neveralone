using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[ExecuteInEditMode]
public class MaxWidth : MonoBehaviour
{
    float maxWidth = 100f;
    public float screenWidth;
    public float screenHeight;
    void Update()
    {
        // this.GetComponent<RectTransform>().sizeDelta;
        // this.GetComponent<RectTransform>().sizeDelta = new Vector2(newWidthUI,newHeightUI);
    }
}
